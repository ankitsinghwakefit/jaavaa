package course.serializationPkg;

public class SerializationType {
    // Serialization
    // whenever we have to transfer some data to different machines we first have to
    // convert it to
    // Byte-stream and it can be saved to database,machine or sent over network and
    // can
    // be sent to
    // other machine via network

    // after transfer again that byte-stream converted to object called
    // deSerialization
}
