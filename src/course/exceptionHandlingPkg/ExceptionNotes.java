package course.exceptionHandlingPkg;

public class ExceptionNotes {
    // 3 types of exceptions

    // compile time error > solved at time of development only
    // logical error > solved by dev

    // runtime error > this is case if it will not be handled it will stop the
    // program
}
